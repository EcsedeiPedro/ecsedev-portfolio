import { About } from "@/components/about";
import { Hero } from "@/components/hero";

export const Home = () => {
  return (
    <main className="hero-gradient">
        <Hero />
        <About />
    </main>
  );
};
